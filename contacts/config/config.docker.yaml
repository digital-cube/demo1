name: 'contacts demo'
description: 'base3 application'
prefix: '/api/contacts'
app_version: '0.0.1'
host: 'contacts'
port: &port 80

store:
  type: 'redis'
  host: 'redis'
  port: '6379'

authorization:
  key: 'Authorization'

db:
  type: 'postgres'
  port: 5432
  host: 'postgres'
  username: 'demo'
  password: 'demo'
  database: 'contacts'

log:
  console: true
  file: '/tmp/log_contacts'
