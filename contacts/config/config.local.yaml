name: 'contacts demo'
description: 'base3 application'
prefix: '/api/contacts'
app_version: '0.0.1'
host: 'localhost'
port: &port 9001

store:
  type: 'redis'

authorization:
  key: 'Authorization'

db:
  type: 'postgres'
  port: 5432
  host: 'localhost'
  username: 'demo'
  password: 'demo'
  database: 'demo_contacts'

log:
  console: true
  file: '/tmp/log_contacts'
